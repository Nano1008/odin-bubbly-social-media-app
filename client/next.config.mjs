/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "cdn.jsdelivr.net", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
